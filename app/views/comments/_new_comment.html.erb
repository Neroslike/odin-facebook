<%= form_with url: create_comment_path(post), method: :post, class: 'new-comment-box', data: {'controller': 'comments'} do |f|%>
  <div class='new-comment-input' data-comments-target='editableDiv'>
    <%= f.text_area 'comment[body]', class: 'comment-input', rows: '1', data: {'comments-target': 'textArea', 'action': 'keydown->comments#submit'}, placeholder: 'Write a comment' %>
  </div>
  <%= f.hidden_field 'comment[user_id]', value: current_user.id %>
  <%= f.hidden_field 'comment[post_id]', value: post.id %>
  <%= button_tag class: 'new-comment-button', data: {'action': 'comments#submitAndCleanup'} do %>
    <i class="fa-solid fa-paper-plane comment-send-icon"></i>
  <% end %>
<% end %>
