<% if @comment.parent_id.nil? %>
  <%= turbo_stream.prepend "comments_#{@comment.post.id}", @comment %>
<% else %>
  <%= turbo_stream.append "comment_#{@comment.parent_id}_replies", @comment %>
<% end %>
